@if(Request.IsAuthenticated) {
    <text>
    <strong>@User.Identity.Name</strong>
    </text>
}
else {
    <text>
        @Html.ActionLink("login", "LogOn", "Account")  @Html.ActionLink("signup", "Register", "Account")

    </text>
}
